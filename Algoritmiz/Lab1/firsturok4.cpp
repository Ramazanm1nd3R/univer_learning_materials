#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    float a, b, c, p;
    c = (pow(a,2))+(pow(b,2));
    c = sqrt(c);
    p = a + b + c;
    cout << "C = " << c << endl;
    cout << "P =" << p;
}